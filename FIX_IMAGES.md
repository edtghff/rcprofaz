# 🔧 Решение проблемы с отображением старых изображений

## Проблема
После замены изображений на сайте все еще отображаются старые изображения.

## Решение (выполните по порядку):

### 1. ✅ Очистка кеша Next.js (УЖЕ ВЫПОЛНЕНО)
```bash
./clear-cache.sh
```
или вручную:
```bash
rm -rf .next
```

### 2. 🔄 Перезапуск dev-сервера

**Остановите текущий сервер** (Ctrl+C в терминале где запущен `npm run dev`)

Затем запустите заново:
```bash
npm run dev
```

### 3. 🌐 Очистка кеша браузера

#### Вариант A: Жесткая перезагрузка страницы
- **Windows/Linux**: `Ctrl + Shift + R` или `Ctrl + F5`
- **Mac**: `Cmd + Shift + R`

#### Вариант B: Очистка через DevTools
1. Откройте DevTools (F12)
2. Перейдите на вкладку **Network**
3. Поставьте галочку **"Disable cache"**
4. Обновите страницу (F5)

#### Вариант C: Полная очистка кеша браузера
- **Chrome**: Настройки → Конфиденциальность → Очистить данные → Кешированные изображения и файлы
- **Firefox**: Настройки → Приватность → Очистить данные → Кеш
- **Safari**: Разработка → Очистить кеши

### 4. 🔍 Проверка файлов

Убедитесь, что файлы действительно заменены:
```bash
ls -lh public/images/services/*.jpg
ls -lh public/images/products/*.jpg
```

Проверьте даты изменения файлов - они должны быть свежими (сегодняшними).

### 5. 🚨 Если проблема сохраняется

#### Проверьте имена файлов
Убедитесь, что имена файлов **точно совпадают** (включая регистр букв):
- ✅ Правильно: `lift-satisi-servisi.jpg`
- ❌ Неправильно: `Lift-Satisi-Servisi.jpg` или `lift_satisi_servisi.jpg`

#### Проверьте расширения файлов
- ✅ Правильно: `.jpg` или `.jpeg`
- ❌ Неправильно: `.JPG` или `.jpeg.jpg`

#### Проверьте, что файлы не повреждены
Попробуйте открыть изображения в просмотрщике изображений - они должны открываться нормально.

#### Временное решение: добавьте версию к URL
Если ничего не помогает, можно временно добавить версию к изображениям в коде:
```tsx
// Вместо:
src="/images/services/lift-satisi-servisi.jpg"

// Используйте:
src="/images/services/lift-satisi-servisi.jpg?v=2"
```

### 6. 📝 Проверка в инкогнито режиме

Откройте сайт в режиме инкогнито (Ctrl+Shift+N) - там кеш браузера не используется.

## Дополнительная информация

- Next.js оптимизирует изображения и кеширует их в `.next/cache/images`
- Браузер также кеширует изображения для ускорения загрузки
- После замены изображений нужно очистить оба кеша

## Быстрая команда для всего процесса:

```bash
# 1. Очистка кеша
./clear-cache.sh

# 2. Перезапуск сервера (в другом терминале)
npm run dev

# 3. Откройте браузер в режиме инкогнито и проверьте
```

